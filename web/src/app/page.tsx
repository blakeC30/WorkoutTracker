/**
 * Placeholder until Phase 4. Charts land here, fed by server-side routes in this app that
 * call the backend — the browser never holds the API secret.
 */
export default function Home() {
  return (
    <main style={{ padding: '2rem', lineHeight: 1.6 }}>
      <h1>WorkoutTracker</h1>
      <p>Dashboard coming in Phase 4.</p>
    </main>
  );
}
